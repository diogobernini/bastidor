## Summary

<!-- What does this PR change, and why? -->

## Checklist

- [ ] `npm test` passes locally (green)
- [ ] Console is clean when run with `ELECTRON_ENABLE_LOGGING=1` (no `Uncaught` or `Refused` errors)
- [ ] New UI strings were added in both i18n languages (`en` and `pt-BR` in `src/i18n.js`)
- [ ] Screenshot attached if the change is visual
